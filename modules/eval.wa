;; eval.wa
;; experiment

(def interaction-environemt (create-env)) ;; create env (a pointer)
(def my-variable-table (variable-table-init)) ;; init variable
(def my-instructions (create-instructions)) ;; create instructions (a pointer)

(def eval
  (fn [exp env]
    (let [o (compile exp my-variable-table env)]
      (walley-compile-and-run-vm o))))
